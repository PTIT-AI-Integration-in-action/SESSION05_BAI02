# BÀI 2: Tối ưu Prompt - Tránh bẫy thời gian tương đối cho AI Agent

## Bối cảnh

Khách hỏi: "Tôi muốn tìm một phòng Deluxe từ ngày mai trong 3 ngày". Vì mô hình AI tách biệt hoàn toàn khỏi hệ thống máy chủ backend, nó không có khái niệm về thời gian thực tế. Nếu không có mốc tham chiếu, AI sẽ bịa ra một ngày ngẫu nhiên hoặc truyền thẳng các chuỗi "ngày mai", "tomorrow", "3 ngày" vào tham số của tool, dẫn tới DateTimeParseException và lỗi HTTP 500.

## System Prompt thiết kế

Đã được xây dựng thành hằng số `SYSTEM_PROMPT` trong Controller, gồm đầy đủ:

- Vai trò: "Bạn là trợ lý đặt phòng khách sạn R-Hotels".
- Nhiệm vụ: giúp kiểm tra phòng trống và tính tổng chi phí.
- Ngữ cảnh thời gian: biến `{today}` chứa ngày hệ thống.
- Ràng buộc: file 6 quy tắc quy đổi ngày tương đối sang yyyy-MM-dd và tuyệt đối không truyền chuỗi tương đối.
- Định dạng đầu ra của tham số ngày: yyyy-MM-dd.

## Mã nguồn Java Controller sau khi tối ưu

Mã nguồn đầy đủ nằm ở file `src/main/java/com/rhotels/controller/BookingController.java`. Điểm mấu chốt của bản vá:

```java
this.chatClient.prompt()
    .system(s -> s.text(SYSTEM_PROMPT).param("today", today.toString()))
    .user(message)
    .call()
    .content();
```

Trong đó `today` được lấy tại thời điểm mỗi request:

```java
LocalDate today = LocalDate.now();
```

## Phân tích & Lập luận kỹ thuật

### Vì sao tiêm biến động loại bỏ lỗi parse ngày

Cơ chế hoạt động như sau. Vấn đề gốc là AI không có đồng hồ. Khi prompt không chứa bất kỳ mốc thời gian nào, model phải tự bịa "ngày mai" là ngày nào, dẫn tới hai kiểu hỏng: (1) chọn một ngày sai ngẫu nhiên, hoặc (2) truyền nguyên chuỗi tương đối "tonorrow" vào tham số. Cả hai đều khiến `LocalDate.parse()` ném DateTimeParseException ở tầng Java.

Bản vá giải quyết bằng cách cung cấp một "mốc tham chiếu toán học" rõ ràng trong context của model. Khi model biết chính xác hôm nay là `2026-08-18`, nó có thể quy đổi "ngày mai" thành `2026-08-19` một cách tất định (deterministic) thay vì đoán. Prompt còn dạy model công thức cộng trừ: checkInDate = ngày hệ thống + 1, checkOutDate = checkInDate + 3. Kết quả đầu ra của model là một chuỗi đúng định dạng yyyy-MM-dd, chính xác là thứ mà `LocalDate.parse()` chấp nhận, nên exception không còn khả năng xảy ra.

### Vì sao phải lấy ngày ở thời điểm request

Tôi lấy `LocalDate.now()` ngay bên trong phương thức `checkRoom`, thay vì lưu một lần từ constructor. Lý do: nếu ứng dụng chạy dài ngày (ví dụ chạy qua nửa đêm, ngày 18 chuyển sang 19), thì một biến tính từ lúc khởi động sẽ trở nên sai lệch và làm model quy đổi sai ngay trên giao dịch thật. Lấy ngày mỗi request bảo đảm mốc tham chiếu luôn là thời điểm thực tế của cuộc hội thoại.

### Loại exception không còn xảy ra

- DateTimeParseException: không còn vì date được đưa vào luôn đúng dạng yyyy-MM-dd.
- NullPointerException do thiếu tham số ngày: được giảm thiểu vì prompt buộc model xác nhận và quy đổi ngày trước khi gọi tool.
- Lỗi HTTP 500 toàn luồng: vì nguyên nhân ném exception đã bị loại, luồng API không còn bị gián đoạn.

Một lưu ý: tiêm mốc thời gian là lớp phòng thủ thứ nhất ở tầng prompt. Lớp phòng thủ thứ hai bắt buộc phải đặt ở tầng thực thi tool với việc kiểm tra lại dữ liệu đầu vào, để kể cả khi model vẫn lỡ truyền dữ liệu lỗi thì hệ thống vẫn không crash. Đó chính là nội dung của Bài 3.